# Public base URL of the Covaga waitlist API (Cloudflare Worker).
# Exposed to the client via import.meta.env.PUBLIC_API_URL.
# Falls back to a placeholder when unset.
PUBLIC_API_URL=https://api.covaga.pages.example
