# Public base URL of the byndr waitlist API (Cloudflare Worker).
# Exposed to the client via import.meta.env.PUBLIC_API_URL.
# Falls back to a placeholder when unset.
PUBLIC_API_URL=https://api.byndr.pages.example
